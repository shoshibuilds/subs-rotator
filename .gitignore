# Sensitive data - NEVER commit these
# (stored in ~/.subs-rotator/, not in this folder - but just in case)
*.bin
rotator_config.json
rotator_config.json.bak

# Python cache
__pycache__/
*.pyc
*.pyo

# PyInstaller build output
dist/
build/
*.spec

